<!DOCTYPE html>
<html lang="fa" dir="rtl">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>فایل‌های سفارش #<%= orderId %>
    </title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600;700&display=swap');

        body {
            font-family: 'Montserrat', sans-serif;
            background: linear-gradient(135deg, #1e293b, #0f172a);
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: flex-start;
            padding: 4rem 1rem;
            position: relative;
            overflow-x: hidden;
        }

        .card {
            background: rgba(255, 255, 255, 0.07);
            backdrop-filter: blur(12px);
            border-radius: 2rem;
            max-width: 650px;
            width: 100%;
            padding: 2.5rem;
            color: #f1f5f9;
            box-shadow: 0 15px 30px rgba(0, 0, 0, 0.5);
        }

        h2 {
            font-weight: 700;
            font-size: 2rem;
            margin-bottom: 0.5rem;
            text-align: center;
        }

        p.subtitle {
            text-align: center;
            color: #cbd5e1;
            margin-bottom: 2rem;
        }

        .input-container {
            display: flex;
            align-items: center;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 1rem;
            padding: 0.75rem 1rem;
            margin-bottom: 1rem;
            border: 1px solid rgba(255, 255, 255, 0.3);
        }

        .input-container i {
            color: #facc15;
            font-size: 1.2rem;
            margin-left: 0.5rem;
        }

        .input-container input {
            flex: 1;
            background: transparent;
            border: none;
            outline: none;
            color: #f1f5f9;
            font-size: 1rem;
        }

        button {
            width: 100%;
            background: linear-gradient(90deg, #facc15, #eab308);
            color: #1e293b;
            font-weight: 600;
            padding: 0.75rem;
            border-radius: 1rem;
            border: none;
            cursor: pointer;
            transition: all 0.25s ease;
        }

        button:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 25px rgba(250, 204, 21, 0.6);
        }

        .file-list {
            margin-top: 2rem;
        }

        .file-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            background: rgba(255, 255, 255, 0.05);
            padding: 0.8rem 1rem;
            border-radius: 1rem;
            margin-bottom: 0.75rem;
            transition: background 0.2s;
        }

        .file-item:hover {
            background: rgba(255, 255, 255, 0.1);
        }

        .file-actions a,
        .file-actions button {
            font-size: 0.85rem;
            padding: 0.4rem 0.7rem;
            border-radius: 0.75rem;
        }

        .file-actions a {
            background-color: #facc15;
            color: #1e293b;
            margin-left: 0.5rem;
            text-decoration: none;
            transition: all 0.2s;
        }

        .file-actions a:hover {
            background-color: #eab308;
        }

        .file-actions button {
            background-color: #f87171;
            color: #fff;
            border: none;
            transition: all 0.2s;
        }

        .file-actions button:hover {
            background-color: #ef4444;
        }

        .flash-message {
            padding: 0.8rem 1rem;
            border-radius: 1rem;
            margin-bottom: 1rem;
            text-align: center;
            font-weight: 600;
        }

        .error-msg {
            background: rgba(248, 113, 113, 0.2);
            color: #f87171;
        }

        .success-msg {
            background: rgba(132, 204, 22, 0.2);
            color: #84cc16;
        }
    </style>
</head>
<%- include('layouts/header') %>

    <body>
        <div class="card">
            <h2>فایل‌های سفارش #<%= orderId %>
            </h2>
            <p class="subtitle">آپلود و مدیریت فایل‌های سفارش</p>

            <% if (typeof error_msg !=='undefined' && error_msg) { %>
                <div class="flash-message error-msg">
                    <%= error_msg %>
                </div>
                <% } %>
                    <% if (typeof success_msg !=='undefined' && success_msg) { %>
                        <div class="flash-message success-msg">
                            <%= success_msg %>
                        </div>
                        <% } %>

                            <form action="/orders/<%= orderId %>/files" method="POST" enctype="multipart/form-data">
                                <div class="input-container">
                                    <i class="fa-solid fa-file-upload"></i>
                                    <input type="file" name="file" required>
                                </div>
                                <button type="submit"><i class="fa-solid fa-upload ml-2"></i> آپلود فایل</button>
                            </form>

                            <div class="file-list">
                                <% if (files.length===0) { %>
                                    <p class="text-gray-400 text-center">هنوز فایلی آپلود نشده است.</p>
                                    <% } else { %>
                                        <% files.forEach(file=> { %>
                                            <div class="file-item">
                                                <span>
                                                    <%= file.filename %> - <%= new
                                                            Date(file.uploaded_at).toLocaleString() %>
                                                </span>
                                                <div class="file-actions flex">
                                                    <a href="/<%= file.filepath %>" target="_blank"><i
                                                            class="fa-solid fa-download ml-1"></i> دانلود</a>
                                                    <form
                                                        action="/orders/<%= orderId %>/files/<%= file.id %>?_method=DELETE"
                                                        method="POST">
                                                        <button type="submit"><i class="fa-solid fa-trash ml-1"></i>
                                                            حذف</button>
                                                    </form>
                                                </div>
                                            </div>
                                            <% }) %>
                                                <% } %>
                            </div>

                            <p class="mt-6 text-center">
                                <a href="/dashboard">بازگشت به داشبورد</a> | <a href="/logout">خروج</a>
                            </p>
        </div>
        <%- include('layouts/footer') %>

    </body>

</html>