<!DOCTYPE html>
<html lang="fa" dir="rtl">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>تأیید حذف سفارش</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://unpkg.com/lucide@latest"></script>
    <style>
        body {
            font-family: 'Vazir', sans-serif;
            background: linear-gradient(135deg, #a8edea, #fed6e3);
            background-attachment: fixed;
            min-height: 100vh;
        }

        .glass-card {
            backdrop-filter: blur(20px);
            background: rgba(255, 255, 255, 0.2);
            border: 1px solid rgba(255, 255, 255, 0.3);
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.15);
            animation: fadeIn 0.5s ease-in-out;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(15px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .btn {
            transition: all 0.3s ease;
        }

        .btn:hover {
            transform: scale(1.05);
        }
    </style>
</head>
<%- include('layouts/header') %>

    <body class="flex items-center justify-center p-6">

        <div class="glass-card p-8 rounded-3xl text-center w-full max-w-md text-gray-800">
            <i data-lucide="alert-triangle" class="mx-auto text-red-600 w-16 h-16 mb-5"></i>
            <h1 class="text-2xl font-extrabold mb-3">تأیید حذف سفارش</h1>

            <p class="text-gray-700 mb-6 leading-relaxed">
                آیا از حذف سفارش
                <strong>#<%= order && order.id ? order.id : '---' %></strong>
                با عنوان "<strong>
                    <%= order && order.title ? order.title : '---' %>
                </strong>" اطمینان دارید؟
                <br>این عمل غیرقابل بازگشت است.
            </p>

            <form action="/orders/delete/<%= order.id %>" method="POST" class="flex flex-col gap-4">
                <button type="submit"
                    class="btn w-full py-3 rounded-xl bg-gradient-to-r from-red-500 to-rose-600 text-white font-bold shadow-lg flex items-center justify-center gap-2">
                    <i data-lucide="trash-2"></i>
                    بله، حذف شود
                </button>
                <a href="/orders"
                    class="btn w-full py-3 rounded-xl bg-white/60 hover:bg-white/80 text-gray-800 font-semibold shadow-md flex items-center justify-center gap-2">
                    <i data-lucide="x"></i>
                    انصراف
                </a>
            </form>
        </div>

        <script>
            lucide.createIcons();
        </script>

    </body>

</html>