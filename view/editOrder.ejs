<!DOCTYPE html>
<html lang="fa" dir="rtl">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <title>ویرایش سفارش #<%= order.id %>
    </title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://unpkg.com/lucide@latest"></script>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600;700&display=swap');

        body {
            font-family: 'Montserrat', sans-serif;
            background: linear-gradient(135deg, #0f172a, #1e293b);
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: flex-start;
            padding-top: 4rem;
            color: #f1f5f9;
        }

        .card {
            backdrop-filter: blur(10px);
            background: rgba(255, 255, 255, 0.05);
            border: 1px solid rgba(255, 255, 255, 0.2);
            border-radius: 2rem;
            padding: 2.5rem;
            max-width: 600px;
            width: 90%;
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.5);
        }

        .input-container {
            display: flex;
            align-items: center;
            margin-bottom: 1rem;
            border-radius: 1rem;
            overflow: hidden;
            border: 1px solid rgba(255, 255, 255, 0.3);
            background: rgba(255, 255, 255, 0.1);
            position: relative;
        }

        .input-container i {
            padding: 0 1rem;
            color: #facc15;
            font-size: 1.2rem;
        }

        .input-container input,
        .input-container select,
        .input-container textarea {
            flex: 1;
            padding: 1rem;
            background: transparent;
            border: none;
            color: #f1f5f9;
            font-size: 1rem;
            outline: none;
        }

        textarea {
            resize: vertical;
        }

        button {
            background: linear-gradient(90deg, #facc15, #eab308);
            padding: 0.75rem 1rem;
            border-radius: 1rem;
            border: none;
            width: 100%;
            font-weight: 600;
            color: #1e293b;
            cursor: pointer;
            transition: transform 0.2s, box-shadow 0.2s;
        }

        button:hover {
            transform: translateY(-3px);
            box-shadow: 0 6px 20px rgba(250, 204, 21, 0.6);
        }
    </style>
</head>
<%- include('layouts/header') %>

    <body>
        <div class="card">
            <h1 class="text-2xl font-bold mb-6 text-center">ویرایش سفارش #<%= order.id %>
            </h1>

            <form action="/orders/edit/<%= order.id %>" method="POST" enctype="multipart/form-data" class="space-y-4">
                <div class="input-container">
                    <i class="fa-solid fa-heading"></i>
                    <input type="text" name="title" value="<%= order.title %>" placeholder="عنوان" required>
                </div>

                <div class="input-container">
                    <i class="fa-solid fa-align-left"></i>
                    <textarea name="description" placeholder="توضیحات" required><%= order.description %></textarea>
                </div>

                <div class="input-container">
                    <i class="fa-solid fa-building"></i>
                    <select name="department" required>
                        <option value="">دپارتمان</option>
                        <option value="Sales" <%=order.department==='Sales' ? 'selected' : '' %>>فروش</option>
                        <option value="Support" <%=order.department==='Support' ? 'selected' : '' %>>پشتیبانی</option>
                        <option value="IT" <%=order.department==='IT' ? 'selected' : '' %>>IT</option>
                        <option value="HR" <%=order.department==='HR' ? 'selected' : '' %>>HR</option>
                    </select>
                </div>

                <div class="input-container">
                    <i class="fa-solid fa-exclamation-circle"></i>
                    <select name="priority" required>
                        <option value="">اولویت</option>
                        <option value="Low" <%=order.priority==='Low' ? 'selected' : '' %>>کم</option>
                        <option value="Medium" <%=order.priority==='Medium' ? 'selected' : '' %>>متوسط</option>
                        <option value="High" <%=order.priority==='High' ? 'selected' : '' %>>زیاد</option>
                    </select>
                </div>

                <div class="input-container">
                    <i class="fa-solid fa-calendar"></i>
                    <input type="date" name="eta"
                        value="<%= order.eta ? (new Date(order.eta)).toISOString().slice(0,10) : '' %>">
                </div>

                <div class="input-container">
                    <i class="fa-solid fa-paperclip"></i>
                    <input type="file" name="attachments" multiple>
                </div>

                <button>بروزرسانی سفارش</button>
            </form>
        </div>

    </body>

</html>