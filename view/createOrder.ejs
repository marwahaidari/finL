<!DOCTYPE html>
<html lang="fa" dir="rtl">

<head>
    <meta charset="UTF-8">
    <title>ایجاد سفارش جدید</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://unpkg.com/lucide@latest"></script>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600;700&display=swap');

        body {
            font-family: 'Montserrat', sans-serif;
            background: linear-gradient(135deg, #0f172a, #1e293b);
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: flex-start;
            padding-top: 4rem;
            color: #f1f5f9;
        }

        .card {
            backdrop-filter: blur(10px);
            background: rgba(255, 255, 255, 0.05);
            border: 1px solid rgba(255, 255, 255, 0.2);
            border-radius: 2rem;
            padding: 2.5rem;
            max-width: 600px;
            width: 90%;
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.5);
        }

        .input-container {
            display: flex;
            align-items: center;
            margin-bottom: 1rem;
            border-radius: 1rem;
            overflow: hidden;
            border: 1px solid rgba(255, 255, 255, 0.3);
            background: rgba(255, 255, 255, 0.1);
            position: relative;
        }

        .input-container i {
            padding: 0 1rem;
            color: #facc15;
            font-size: 1.2rem;
        }

        .input-container input,
        .input-container select,
        .input-container textarea {
            flex: 1;
            padding: 1rem;
            background: transparent;
            border: none;
            color: #f1f5f9;
            font-size: 1rem;
            outline: none;
        }

        textarea {
            resize: vertical;
        }

        button {
            background: linear-gradient(90deg, #facc15, #eab308);
            padding: 0.75rem 1rem;
            border-radius: 1rem;
            border: none;
            width: 100%;
            font-weight: 600;
            color: #1e293b;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 0.5rem;
            transition: transform 0.2s, box-shadow 0.2s;
        }

        button:hover {
            transform: translateY(-3px);
            box-shadow: 0 6px 20px rgba(250, 204, 21, 0.6);
        }

        a {
            color: #facc15;
            text-decoration: underline;
        }

        .flash-message {
            padding: 0.75rem 1rem;
            border-radius: 1rem;
            margin-bottom: 1rem;
        }

        .error-msg {
            background: rgba(248, 113, 113, 0.2);
            color: #f87171;
        }

        .success-msg {
            background: rgba(132, 204, 22, 0.2);
            color: #84cc16;
        }
    </style>
</head>

<body>
    <div class="card">
        <h1 class="text-2xl font-bold mb-6 text-center flex items-center justify-center gap-2">
            <i data-lucide="plus-circle"></i> ایجاد سفارش جدید
        </h1>

        <% if (typeof error_msg !=='undefined' && error_msg) { %>
            <div class="flash-message error-msg">
                <%= error_msg %>
            </div>
            <% } %>
                <% if (typeof success_msg !=='undefined' && success_msg) { %>
                    <div class="flash-message success-msg">
                        <%= success_msg %>
                    </div>
                    <% } %>

                        <form action="/orders/create" method="POST" enctype="multipart/form-data" class="space-y-4">
                            <% const old=typeof oldInput !=='undefined' ? oldInput : {}; %>

                                <div class="input-container">
                                    <i class="fa-solid fa-heading"></i>
                                    <input type="text" name="title" placeholder="عنوان سفارش" required
                                        value="<%= old.title || '' %>">
                                </div>

                                <div class="input-container">
                                    <i class="fa-solid fa-align-left"></i>
                                    <textarea name="description" placeholder="توضیحات"
                                        required><%= old.description || '' %></textarea>
                                </div>

                                <div class="input-container">
                                    <i class="fa-solid fa-building"></i>
                                    <select name="department" required>
                                        <option value="">دپارتمان</option>
                                        <option value="Sales" <%=old.department==='Sales' ? 'selected' : '' %>>فروش
                                        </option>
                                        <option value="Support" <%=old.department==='Support' ? 'selected' : '' %>
                                            >پشتیبانی</option>
                                        <option value="IT" <%=old.department==='IT' ? 'selected' : '' %>>IT</option>
                                        <option value="HR" <%=old.department==='HR' ? 'selected' : '' %>>HR</option>
                                    </select>
                                </div>

                                <div class="input-container">
                                    <i class="fa-solid fa-exclamation-circle"></i>
                                    <select name="priority" required>
                                        <option value="">اولویت</option>
                                        <option value="Low" <%=old.priority==='Low' ? 'selected' : '' %>>کم</option>
                                        <option value="Medium" <%=old.priority==='Medium' ? 'selected' : '' %>>متوسط
                                        </option>
                                        <option value="High" <%=old.priority==='High' ? 'selected' : '' %>>زیاد</option>
                                    </select>
                                </div>

                                <div class="input-container">
                                    <i class="fa-solid fa-calendar"></i>
                                    <input type="date" name="eta" value="<%= old.eta || '' %>">
                                </div>

                                <div class="input-container">
                                    <i class="fa-solid fa-paperclip"></i>
                                    <input type="file" name="attachments" multiple>
                                </div>

                                <button><i data-lucide="check-circle"></i> ثبت سفارش</button>
                        </form>

                        <p class="mt-4 text-center">
                            <a href="/orders">بازگشت به لیست سفارشات</a>
                        </p>
    </div>

    <script>lucide.createIcons();</script>
    <%- include('layouts/footer') %>

</body>

</html>