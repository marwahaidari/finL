<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Enable 2FA | Government Citizen Portal</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(135deg, #0dcaf0, #198754);
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        .card {
            max-width: 500px;
            width: 100%;
            padding: 30px;
            border-radius: 15px;
            background: #fff;
            box-shadow: 0 5px 20px rgba(0, 0, 0, 0.2);
        }

        .alert {
            border-radius: 10px;
        }

        .qr-code {
            width: 200px;
            height: 200px;
            margin: 20px auto;
        }
    </style>
</head>

<body>
    <div class="card text-center">
        <h4>Enable Two-Factor Authentication (2FA)</h4>
        <p>Scan this QR code with your authenticator app</p>

        <img src="<%= qrCodeDataUrl %>" alt="QR Code" class="qr-code">

        <form action="/2fa/verify" method="POST">
            <div class="mb-3">
                <input type="text" name="token" class="form-control" placeholder="Enter OTP from app" required>
            </div>
            <button type="submit" class="btn btn-success w-100">Verify & Enable 2FA</button>
        </form>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>