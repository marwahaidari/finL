<!DOCTYPE html>
<html lang="fa" dir="rtl">

<head>
    <meta charset="UTF-8">
    <title>جزئیات سفارش #<%= order && order.id ? order.id : '---' %>
    </title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="/socket.io/socket.io.js"></script>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://unpkg.com/lucide@latest"></script>
    <style>
        body {
            font-family: Vazir, sans-serif;
            background-color: #f0f4f8;
        }

        .card {
            background-color: #fff;
            border-radius: 1rem;
            padding: 1.5rem;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.05);
            transition: all 0.3s ease-in-out;
        }

        .card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
        }

        .badge {
            display: inline-block;
            padding: 0.25rem 0.75rem;
            border-radius: 9999px;
            font-weight: 600;
            font-size: 0.875rem;
        }

        .badge-paid {
            background-color: #10b981;
            color: white;
        }

        .badge-unpaid {
            background-color: #ef4444;
            color: white;
        }

        .timeline {
            border-right: 3px solid #3b82f6;
            margin-right: 1rem;
            padding-right: 1rem;
        }

        .timeline-item {
            margin-bottom: 1rem;
            position: relative;
        }

        .timeline-item::before {
            content: '';
            position: absolute;
            right: -10px;
            top: 0;
            width: 1rem;
            height: 1rem;
            background-color: #3b82f6;
            border-radius: 50%;
            border: 3px solid #fff;
        }
    </style>
</head>

<body class="p-6">

    <!-- Header -->
    <header class="flex justify-between items-center mb-6">
        <h1 class="text-2xl font-bold flex items-center gap-2">
            <i data-lucide="clipboard"></i> جزئیات سفارش #<%= order && order.id ? order.id : '---' %>
        </h1>
        <a href="/orders" class="text-blue-600 hover:underline flex items-center gap-1">
            <i data-lucide="arrow-left"></i> بازگشت به لیست
        </a>
    </header>

    <!-- اطلاعات سفارش -->
    <section class="card mb-6">
        <h2 class="text-xl font-semibold mb-4">اطلاعات سفارش</h2>
        <p><strong>عنوان:</strong>
            <%= order && order.title ? order.title : '---' %>
        </p>
        <p><strong>توضیحات:</strong>
            <%= order && order.description ? order.description : '---' %>
        </p>
        <p><strong>کاربر:</strong>
            <%= order && (order.user_name || (order.user && order.user.full_name)) ? (order.user_name ||
                order.user.full_name) : '---' %>
        </p>
        <p><strong>تاریخ ایجاد:</strong>
            <%= order && order.created_at ? new Date(order.created_at).toLocaleString('fa-IR') : '---' %>
        </p>

        <p><strong>وضعیت:</strong>
            <% if(order && order.status==='pending' ){ %>
                <span class="badge bg-yellow-500 text-white">در حال انجام ⚠️</span>
                <% } else if(order && order.status==='completed' ){ %>
                    <span class="badge bg-blue-500 text-white">تکمیل‌شده ✅</span>
                    <% } else { %>
                        <span class="badge bg-red-500 text-white">لغو شده ❌</span>
                        <% } %>
        </p>

        <!-- وضعیت پرداخت -->
        <p id="payment-status" class="mt-4"><strong>پرداخت:</strong>
            <% if(order && order.paid){ %>
                <span class="badge badge-paid">پرداخت شده ✅</span>
                <% } else { %>
                    <span class="badge badge-unpaid">پرداخت نشده ❌</span>
                    <% } %>
        </p>

        <% if(order && !order.paid){ %>
            <form action="/orders/pay/<%= order.id %>" method="POST" class="mt-4">
                <button class="px-4 py-2 bg-green-600 text-white rounded-lg hover:bg-green-700 transition">
                    پرداخت سفارش
                </button>
            </form>
            <% } %>
    </section>

    <!-- فایل‌های پیوست -->
    <% if(order && order.attachments && order.attachments.length>0){ %>
        <section class="card mb-6">
            <h2 class="text-xl font-semibold mb-4">فایل‌های پیوست</h2>
            <ul class="space-y-2">
                <% order.attachments.forEach(file=> { %>
                    <li>
                        <a href="/uploads/<%= file.filename %>" target="_blank"
                            class="text-blue-600 hover:underline flex items-center gap-2">
                            <i data-lucide="file-text"></i>
                            <%= file.originalname %>
                        </a>
                    </li>
                    <% }) %>
            </ul>
        </section>
        <% } %>

            <!-- تاریخچه سفارش -->
            <% if(order && order.history && order.history.length>0){ %>
                <section class="card mb-6">
                    <h2 class="text-xl font-semibold mb-4">تاریخچه وضعیت سفارش</h2>
                    <div class="timeline">
                        <% order.history.forEach(item=> { %>
                            <div class="timeline-item">
                                <p><strong>
                                        <%= item.status %>
                                    </strong> - <%= new Date(item.date).toLocaleString('fa-IR') %>
                                </p>
                            </div>
                            <% }) %>
                    </div>
                </section>
                <% } %>

                    <!-- بازخوردها -->
                    <% if(reviews && reviews.length>0){ %>
                        <section class="card mb-6">
                            <h2 class="text-xl font-semibold mb-4">بازخوردها</h2>
                            <ul class="space-y-4">
                                <% reviews.forEach(r=> { %>
                                    <li class="border p-3 rounded-lg hover:shadow-md transition duration-150">
                                        <p><strong>
                                                <%= r.username %>
                                            </strong> ⭐ <%= r.rating %>/5</p>
                                        <p class="text-gray-700">
                                            <%= r.comment %>
                                        </p>
                                    </li>
                                    <% }) %>
                            </ul>
                        </section>
                        <% } %>

                            <!-- دکمه‌های عملیاتی -->
                            <section class="card mb-6 flex gap-4">
                                <a href="/orders/edit/<%= order && order.id ? order.id : '#' %>"
                                    class="px-4 py-2 bg-yellow-500 text-white rounded-lg hover:bg-yellow-600 transition flex items-center gap-2">
                                    <i data-lucide="edit-2"></i> ویرایش سفارش
                                </a>
                                <form action="/orders/delete/<%= order && order.id ? order.id : '#' %>" method="POST"
                                    onsubmit="return confirm('آیا مطمئن هستید؟')" class="inline">
                                    <button
                                        class="px-4 py-2 bg-red-600 text-white rounded-lg hover:bg-red-700 transition flex items-center gap-2">
                                        <i data-lucide="trash-2"></i> حذف سفارش
                                    </button>
                                </form>
                            </section>

                            <script>
                                lucide.createIcons();
                                const socket = io();
                                const currentOrderId = "<%= order && order.id ? order.id : '' %>";

                                socket.on('orderPaid', (updatedOrder) => {
                                    if (String(updatedOrder.id) === currentOrderId) {
                                        const paymentStatus = document.getElementById('payment-status');
                                        paymentStatus.innerHTML = `<strong>پرداخت:</strong> <span class="badge badge-paid">پرداخت شده ✅</span>`;
                                    }
                                });
                            </script>

</body>

</html>