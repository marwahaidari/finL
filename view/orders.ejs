<!DOCTYPE html>
<html lang="fa" dir="rtl">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>لیست سفارشات</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://unpkg.com/lucide@latest"></script>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600;700&display=swap');

        body {
            font-family: 'Montserrat', sans-serif;
            background: linear-gradient(135deg, #0f172a, #1e293b);
            min-height: 100vh;
            padding: 2rem;
            color: #f1f5f9;
        }

        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 2rem;
        }

        .header h1 {
            font-size: 2rem;
            font-weight: 700;
        }

        .header a {
            background: linear-gradient(90deg, #facc15, #eab308);
            padding: 0.75rem 1.5rem;
            border-radius: 1rem;
            color: #1e293b;
            font-weight: 600;
            transition: transform 0.25s, box-shadow 0.25s;
        }

        .header a:hover {
            transform: translateY(-3px) scale(1.05);
            box-shadow: 0 8px 25px rgba(250, 204, 21, 0.5);
        }

        .order-card {
            backdrop-filter: blur(8px);
            background: rgba(255, 255, 255, 0.05);
            border-radius: 1.5rem;
            padding: 1.5rem;
            margin-bottom: 1.5rem;
            border: 1px solid rgba(255, 255, 255, 0.2);
            box-shadow: 0 12px 30px rgba(0, 0, 0, 0.4);
            transition: transform 0.25s, box-shadow 0.25s;
        }

        .order-card:hover {
            transform: translateY(-4px);
            box-shadow: 0 16px 35px rgba(250, 204, 21, 0.6);
        }

        .order-title {
            font-size: 1.25rem;
            font-weight: 600;
            margin-bottom: 0.5rem;
        }

        .order-info {
            display: flex;
            justify-content: space-between;
            margin-bottom: 0.5rem;
            font-size: 0.9rem;
            color: #cbd5e1;
        }

        .badge {
            display: inline-flex;
            align-items: center;
            padding: 0.25rem 0.6rem;
            border-radius: 9999px;
            font-weight: 600;
            font-size: 0.8rem;
        }

        .badge.pending {
            background: #f59e0b;
            color: white;
        }

        .badge.completed {
            background: #3b82f6;
            color: white;
        }

        .badge.other {
            background: #ef4444;
            color: white;
        }

        .badge.paid {
            background: #10b981;
            color: white;
        }

        .badge.unpaid {
            background: #ef4444;
            color: white;
        }

        .actions {
            display: flex;
            gap: 0.5rem;
            margin-top: 1rem;
        }

        .actions a {
            flex: 1;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 0.5rem;
            border-radius: 1rem;
            font-weight: 600;
            transition: transform 0.25s, box-shadow 0.25s;
        }

        .actions a.edit {
            background: #facc15;
            color: #1e293b;
        }

        .actions a.edit:hover {
            transform: translateY(-2px) scale(1.02);
            box-shadow: 0 6px 20px rgba(250, 204, 21, 0.5);
        }

        .actions a.delete {
            background: #ef4444;
            color: white;
        }

        .actions a.delete:hover {
            transform: translateY(-2px) scale(1.02);
            box-shadow: 0 6px 20px rgba(239, 68, 68, 0.5);
        }

        .flash-message {
            padding: 0.75rem 1rem;
            border-radius: 1rem;
            margin-bottom: 1rem;
        }

        .error-msg {
            background: rgba(248, 113, 113, 0.2);
            color: #f87171;
        }

        .success-msg {
            background: rgba(132, 204, 22, 0.2);
            color: #84cc16;
        }
    </style>
</head>
<%- include('layouts/header') %>

    <body>

        <div class="header">
            <h1>لیست سفارشات</h1>
            <a href="/orders/create"><i data-lucide="plus-circle"></i> ایجاد سفارش جدید</a>
        </div>

        <% if (typeof error_msg !=='undefined' && error_msg) { %>
            <div class="flash-message error-msg">
                <%= error_msg %>
            </div>
            <% } %>
                <% if (typeof success_msg !=='undefined' && success_msg) { %>
                    <div class="flash-message success-msg">
                        <%= success_msg %>
                    </div>
                    <% } %>

                        <div class="orders-list">
                            <% orders.forEach(order=> { %>
                                <div class="order-card">
                                    <div class="order-title">
                                        <%= order.title %>
                                    </div>
                                    <div class="order-info">
                                        <span>کاربر: <strong>
                                                <%= order.user_name %>
                                            </strong></span>
                                        <span>ایجاد: <%= new Date(order.created_at).toLocaleString('fa-IR') %></span>
                                    </div>
                                    <div class="order-info">
                                        <span>وضعیت:
                                            <% if(order.status.toLowerCase()==='pending' ){ %>
                                                <span class="badge pending">در حال انجام</span>
                                                <% } else if(order.status.toLowerCase()==='completed' ){ %>
                                                    <span class="badge completed">تکمیل‌شده</span>
                                                    <% } else { %>
                                                        <span class="badge other">
                                                            <%= order.status %>
                                                        </span>
                                                        <% } %>
                                        </span>
                                        <span>پرداخت:
                                            <% if(order.paid){ %>
                                                <span class="badge paid">✅ پرداخت شده</span>
                                                <% } else { %>
                                                    <span class="badge unpaid">❌ پرداخت نشده</span>
                                                    <% } %>
                                        </span>
                                    </div>
                                    <div class="actions">
                                        <a href="/orders/edit/<%= order.id %>" class="edit"><i data-lucide="edit-2"></i>
                                            ویرایش</a>
                                        <a href="/orders/delete/<%= order.id %>" class="delete"><i
                                                data-lucide="trash-2"></i> حذف</a>
                                    </div>
                                </div>
                                <% }) %>
                        </div>

                        <script>lucide.createIcons();</script>
                        <%- include('layouts/footer') %>

    </body>

</html>