<!DOCTYPE html>
<html lang="fa" dir="rtl">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= department ? 'ویرایش دپارتمان' : 'دپارتمان جدید' %>
    </title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-100 p-6 font-sans">

    <div class="max-w-2xl mx-auto bg-white p-6 rounded shadow">
        <h1 class="text-2xl font-bold mb-4">
            <%= department ? 'ویرایش دپارتمان' : 'دپارتمان جدید' %>
        </h1>

        <% if(errors && errors.length> 0) { %>
            <div class="mb-4 bg-red-100 text-red-700 p-3 rounded">
                <ul>
                    <% errors.forEach(err=> { %>
                        <li>
                            <%= err.msg %>
                        </li>
                        <% }) %>
                </ul>
            </div>
            <% } %>

                <form method="POST"
                    action="<%= department ? '/department/' + department.id + '?_method=PUT' : '/department' %>">
                    <div class="mb-4">
                        <label class="block mb-1 font-medium">نام دپارتمان</label>
                        <input type="text" name="name" value="<%= department ? department.name : '' %>" required
                            class="w-full p-2 border rounded">
                    </div>

                    <div class="mb-4">
                        <label class="block mb-1 font-medium">توضیح</label>
                        <textarea name="description" class="w-full p-2 border rounded"
                            rows="3"><%= department ? department.description : '' %></textarea>
                    </div>

                    <div class="flex justify-between items-center">
                        <button type="submit" class="bg-green-600 text-white px-4 py-2 rounded hover:bg-green-700">
                            <%= department ? 'به‌روزرسانی' : 'ایجاد' %>
                        </button>
                        <a href="/department"
                            class="bg-gray-400 text-white px-4 py-2 rounded hover:bg-gray-500">بازگشت</a>
                    </div>
                </form>
    </div>

</body>

</html>